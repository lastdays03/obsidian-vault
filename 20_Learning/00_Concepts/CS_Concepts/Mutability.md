# 개념 (Concept): 가변성 (Mutability)

**태그**: #knowledge/concept #topic/CS #topic/Python
**출처**: [[01. python_variables_datatypes]]

---

## 📖 정의 (Definition)
**가변성(Mutability)**이란 객체가 생성된 후에 그 상태(값)를 변경할 수 있는 성질을 의미합니다. 반대로 값을 변경할 수 없는 성질은 **불변성(Immutability)**이라고 합니다.

---

## 💡 예시 (Example)
파이썬에서 리스트는 가변(Mutable)이고, 튜플은 불변(Immutable)입니다.

```python
# Mutable (List)
my_list = [1, 2, 3]
my_list[0] = 99  # OK: [99, 2, 3]

# Immutable (Tuple)
my_tuple = (1, 2, 3)
# my_tuple[0] = 99  # Error: TypeError 발생
```

| **구분** | **설명** | **예시 (Python)** |
| :--- | :--- | :--- |
| **Mutable** | 생성 후 값 변경 가능 | `list`, `dict`, `set`, `bytearray` |
| **Immutable** | 생성 후 값 변경 불가 (새 객체 생성됨) | `int`, `float`, `str`, `tuple`, `bool`, `frozenset` |

## 💡 Key Insights
- **참조(Reference)와 Side Effect**: 가변 객체는 여러 변수가 같은 객체를 참조할 때, 한쪽에서 수정하면 다른 쪽에도 영향을 미칩니다(Aliasing).
- **성능**: 불변 객체는 Thread-safe하며 최적화에 유리하지만, 값을 바꿀 때마다 새 객체를 만들어야 하므로 빈번한 수정 시에는 가변 객체가 유리할 수 있습니다.
- **해시 가능성(Hashability)**: 딕셔너리의 키(Key)나 집합(Set)의 원소가 되려면 불변(Immutable)이어야 합니다.
