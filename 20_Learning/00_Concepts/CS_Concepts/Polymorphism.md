---
tags: [knowledge/concept, topic/CS, topic/OOP]
Up: [[Learning_MOC]]
---

# 개념 (Concept): 다형성 (Polymorphism)

**출처**: [[07. python_classes]]

---

## 📖 정의 (Definition)
**다형성(Polymorphism)**은 "여러 가지 형태를 가질 수 있는 능력"을 의미하며, 하나의 인터페이스나 메서드 호출이 객체의 타입에 따라 다르게 동작하도록 하는 객체 지향 프로그래밍(OOP)의 핵심 원칙입니다.

---

## 💡 예시 (Example)
파이썬에서는 **메서드 오버라이딩(Method Overriding)**이 다형성의 대표적인 예입니다.

```python
class Dog:
    def speak(self):
        return "멍멍!"

class Cat:
    def speak(self):
        return "야옹!"

def animal_sound(animal):
    # animal 객체의 타입에 상관없이 speak() 메서드 호출
    print(animal.speak())

animals = [Dog(), Cat()]
for a in animals:
    animal_sound(a)  # 객체에 따라 다른 소리 출력 (다형성)
```

| **구분** | **설명** |
| :--- | :--- |
| **오버라이딩 (Overriding)** | 부모 클래스의 메서드를 자식 클래스에서 재정의하는 것 (Runtime Polymorphism) |
| **오버로딩 (Overloading)** | 같은 이름의 메서드를 매개변수의 개수나 타입에 따라 여러 개 정의하는 것 (Python은 공식적으로 지원하지 않음, 기본값 등을 이용해 유사 구현) |

## 💡 Key Insights
- **유연성 및 확장성**: 새로운 클래스가 추가되어도 기존 코드를 수정할 필요 없이 공통 인터페이스만 준수하면 됩니다(OCP: Open-Closed Principle).
- **Duck Typing**: 파이썬은 동적 타이핑 언어이므로, 상속 관계가 아니더라도 같은 메서드(예: `speak()`)를 가지고 있다면 다형성을 구현할 수 있습니다.
