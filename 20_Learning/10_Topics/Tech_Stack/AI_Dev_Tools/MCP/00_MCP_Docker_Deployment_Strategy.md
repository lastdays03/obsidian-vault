# 개념 (Concept): MCP Docker Deployment Strategy

**태그**: #knowledge/concept #topic/AI_Dev_Tools #tech_stack/MCP #devops/docker
**출처**: User Input

---

## 📖 정의 (Definition)
**MCP Docker Deployment Strategy**는 Claude Desktop 등의 클라이언트에서 MCP 서버를 직접 로컬 프로세스(`npx`)로 실행하는 대신, **Docker 컨테이너** 내에서 격리하여 실행하는 배포 방식이다. 2026년 기준, 환경 일관성과 보안을 중시하는 팀이나 헤비 유저들 사이에서 표준으로 자리 잡고 있다.

---

## 💡 장단점 분석 (Pros & Cons)

### 장점 (Pros)
| 순위  | 장점            | 설명                                                                 |
| :---- | :-------------- | :------------------------------------------------------------------- |
| **1** | **완벽한 격리** | Node/Python 버전 충돌 및 글로벌 패키지 오염 방지.                    |
| **2** | **설치 편의성** | `docker pull` 및 `claude mcp add`로 간편 설정 (공식 이미지 활용).    |
| **3** | **공식 지원**   | Docker Desktop의 MCP Toolkit 등 플랫폼 차원의 통합 지원 강화.        |
| **4** | **보안성**      | 컨테이너 샌드박싱으로 호스트 파일 시스템 보호 (Filesystem MCP 제외). |
| **5** | **확장성**      | Docker Compose를 통해 여러 MCP 서버를 단일 설정으로 관리 가능.       |

### 단점 (Cons)
| 순위  | 단점                | 설명                                                             |
| :---- | :------------------ | :--------------------------------------------------------------- |
| **1** | **리소스/속도**     | Docker 실행 오버헤드 존재, 컨테이너 시작(Spin-up)에 3~12초 소요. |
| **2** | **복잡성**          | Docker 기본 지식(Volume, Network 등) 필요.                       |
| **3** | **Filesystem 제약** | 호스트 파일 접근 시 마운트 설정이 번거롭고 권한 문제 발생 가능.  |

---

## ⚖️ 선택 가이드 (Recommendations)

| 상황                              | 추천 방식           | 이유                                                     |
| :-------------------------------- | :------------------ | :------------------------------------------------------- |
| **일반 사용자 / Filesystem 위주** | **`npx` (Local)**   | 설정이 즉각적이고 파일 접근 권한 관리가 직관적임.        |
| **팀 개발 / API 연동 위주**       | **Docker**          | 환경 일관성 보장, 공식 이미지의 빠른 업데이트.           |
| **복합 구성 (Multiple Servers)**  | **Docker Compose**  | 관리 용이성 및 설정 파일 간소화.                         |
| **초기 진입**                     | **`npx` -> Docker** | 가볍게 시작 후 필요성(격리, 관리)이 느껴질 때 전환 추천. |

---

## 🔑 Key Insights
- **"이미 Docker를 쓴다면 Docker로, 아니라면 npx로"**: 2026년 현재 가장 현실적인 조언이다.
- **Filesystem MCP의 특수성**: 파일 시스템 접근이 주 목적이라면 Docker의 격리성이 오히려 방해(마운트 설정 복잡)가 되므로 `npx`가 유리할 수 있다.
- **표준화 흐름**: GitHub 등 주요 벤더들이 Docker 이미지를 공식 배포 수단으로 밀고 있어, 장기적으로는 Docker 방식이 메인스트림이 될 전망이다.

## 📚 References
- [Docker MCP Toolkit](https://www.docker.com/products/mcp-toolkit)
- [GitHub MCP Server Official Image](https://github.com/github/github-mcp-server/pkgs/container/github-mcp-server)
